#include <stdio.h>

int main()
{
    int i,j,n;
    char l;
    printf("enter no.of rows:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        for(j=i;j<=i;j++)
        {
            printf("%c",'v'+j);
            
        }
        printf("\n");
    }

    return 0;
}

